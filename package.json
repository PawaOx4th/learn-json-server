{
  "name": "learn-json-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "json-server --watch db.json --port=9090"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
